name: "Testing"
on:
  issues:
    types: [opened]

jobs:
  blub:
    runs-on: ubuntu-latest
    steps:
    - name: Show issue
      run: |
        echo "${{ github.event.issue.title }}"
    - name: Show Body
      run: |
        echo "${{ toJSON(github.event.issue.body) }}"
